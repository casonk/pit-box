# Repo-local auto-pass defaults for pit-box.
# Copy to config/auto-pass.ini to keep these overrides out of git.

[auto_pass]
profile = infra
env_file = ../auto-pass/config/auto-pass.env.local

[remote_desktop]
web_password_keepass_entry = pit-box/remote-desktop/guacamole
